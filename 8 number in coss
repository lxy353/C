#include <iostream>
#include <cmath>
using namespace std;

bool ok(int q[8], int c) {
     for(int i =0; i <c;i++){
         ifq[c]==q[i] 
             return false;
     for(int c =0;adj_table[8][5]!=-1;i++){
     }
}
     return true;
}

int backtrack(int c){
    c--;
    if(c == -1){
        exit (0);
    }
    return c;      
}

void print(int c[]) {
static int count = 0;
cout << " Soultion number: " << ++count << endl;
cout << " " << cross[1] << cross[2] << endl;
cout << cross[0] << cross[3] << cross[4] << cross[5] << endl;
cout << " " << cross[6] << cross[7] << endl;

}



    return 0;
}
